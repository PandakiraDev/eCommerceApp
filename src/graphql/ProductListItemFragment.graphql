fragment ProductListItemFragment on Product {
  id
  name
  description
  categories(first: 1) {
    name
  }
  images(first: 1) {
    url
  }
  price
  variants {
    ... on ProductSizeColorVariant {
      id
      name
      color
      size
    }
  }
}
