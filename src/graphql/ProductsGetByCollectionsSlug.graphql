query ProductsGetByCollectionsSlug($slug: String!) {
  collections(where: { slug: $slug }) {
    products(first: 10) {
      id
      name
      description
      categories(first: 1) {
        name
        id
      }
      images(first: 1) {
        url
      }
      price
    }
  }
}
